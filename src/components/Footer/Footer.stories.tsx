import React from "react";
import Footer from "./Footer";

export default { title: "components/Footer", component: Footer };

export const asFooter = (): React.ReactNode => <Footer>Hello, world!</Footer>;
